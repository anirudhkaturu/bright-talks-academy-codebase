<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Mr. Kiran Karampudi | Lead Trainer | THE BRIGHT TALKS ACADEMY</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">

  <style>
    /* ------------------------------ ROOT VARIABLES --------------------------------*/
    :root {
      --primary: #0f172a;
      --primary-light: #1e293b;
      --accent: #f59e0b;
      --accent-light: #fbbf24;
      --accent-gradient: linear-gradient(135deg, #f59e0b 0%, #fbbf24 100%);
      --text: #0f172a;
      --text-light: #334155;
      --muted: #64748b;
      --light: #f8fafc;
      --white: #ffffff;
      --border: #e2e8f0;
      --radius-sm: 8px;
      --radius-md: 12px;
      --radius-lg: 16px;
      --radius-xl: 24px;
      --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
      --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
      --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
      --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1);

      --font-sans: 'Inter', system-ui, -apple-system, sans-serif;
      --font-display: 'Playfair Display', Georgia, serif;
    }

    /* ------------------------------ RESET & BASE --------------------------------*/
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: var(--font-sans);
      background: var(--light);
      color: var(--text);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    /* ------------------------------ HERO SECTION --------------------------------*/
    .trainer-hero {
      position: relative;
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
      padding: 4rem 1.5rem 6rem;
      overflow: hidden;
      text-align: center;
    }

    .trainer-hero::before {
      content: '';
      position: absolute;
      top: -30%;
      left: -10%;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(245, 158, 11, 0.15) 0%, transparent 70%);
      pointer-events: none;
    }

    .trainer-hero-content {
      position: relative;
      z-index: 1;
      max-width: 800px;
      margin: 0 auto;
    }

    .trainer-avatar {
      width: 120px;
      height: 120px;
      background: var(--accent-gradient);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem;
      border: 3px solid rgba(255, 255, 255, 0.2);
      box-shadow: 0 15px 30px -10px rgba(245, 158, 11, 0.3);
      position: relative;
    }

    .trainer-avatar::after {
      content: '👨‍🏫';
      font-size: 3.5rem;
    }

    .badge-affiliation {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      background: rgba(245, 158, 11, 0.2);
      color: var(--accent);
      padding: 0.4rem 0.875rem;
      border-radius: var(--radius-sm);
      font-size: 0.8125rem;
      font-weight: 600;
      margin-bottom: 1rem;
      border: 1px solid rgba(245, 158, 11, 0.3);
    }

    .trainer-hero h1 {
      font-family: var(--font-display);
      font-size: clamp(2rem, 5vw, 3rem);
      font-weight: 700;
      color: white;
      margin-bottom: 0.75rem;
      letter-spacing: -0.02em;
    }

    .trainer-title {
      font-size: 1.1rem;
      color: rgba(255, 255, 255, 0.9);
      margin-bottom: 1.5rem;
      line-height: 1.4;
    }

    .trainer-stats {
      display: flex;
      justify-content: center;
      gap: 2rem;
      flex-wrap: wrap;
      margin-top: 1.5rem;
    }

    .stat-box {
      text-align: center;
    }

    .stat-number {
      font-family: var(--font-display);
      font-size: 2.25rem;
      font-weight: 700;
      color: var(--accent);
      line-height: 1;
    }

    .stat-label {
      font-size: 0.75rem;
      color: rgba(255, 255, 255, 0.8);
      margin-top: 0.25rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .hero-wave {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 60px;
      background: var(--light);
      clip-path: polygon(0 100%, 100% 100%, 100% 30%, 0 100%);
    }

    /* ------------------------------ CONTENT SECTIONS --------------------------------*/
    .section {
      max-width: 1200px;
      margin: 0 auto;
      padding: 3rem 1.5rem;
    }

    .section-header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .section-label {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.8125rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--accent);
      margin-bottom: 0.75rem;
    }

    .section-label::before {
      content: '';
      width: 20px;
      height: 2px;
      background: var(--accent);
    }

    .section-title {
      font-family: var(--font-display);
      font-size: clamp(1.5rem, 3vw, 2rem);
      color: var(--primary);
      margin-bottom: 0.5rem;
    }

    .section-subtitle {
      color: var(--muted);
      font-size: 1rem;
      max-width: 600px;
      margin: 0 auto;
      line-height: 1.5;
    }

    /* ------------------------------ ABOUT SECTION --------------------------------*/
    .about-section {
      background: var(--white);
      margin-top: -2rem;
      position: relative;
      z-index: 10;
      border-radius: var(--radius-xl) var(--radius-xl) 0 0;
      padding: 2.5rem 1.5rem;
      box-shadow: var(--shadow-lg);
    }

    .about-content {
      max-width: 700px;
      margin: 0 auto;
      text-align: center;
    }

    .lead-text {
      font-family: var(--font-display);
      font-size: 1.25rem;
      color: var(--primary);
      line-height: 1.5;
      margin-bottom: 1rem;
      font-style: italic;
    }

    .about-text {
      font-size: 1rem;
      color: var(--text-light);
      line-height: 1.7;
      margin-bottom: 1rem;
    }

    .certification-highlight {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      background: linear-gradient(to right, #fffbeb, #fef3c7);
      padding: 0.75rem 1.25rem;
      border-radius: var(--radius-md);
      margin-top: 1rem;
      border: 1px solid var(--accent);
      font-weight: 600;
      color: var(--primary);
      font-size: 0.9375rem;
    }

    /* ------------------------------ CERTIFICATIONS --------------------------------*/
    .certs-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1.25rem;
      margin-top: 2rem;
    }

    .cert-card {
      background: var(--white);
      border-radius: var(--radius-lg);
      padding: 1.75rem;
      box-shadow: var(--shadow-md);
      border: 1px solid var(--border);
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .cert-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: var(--accent-gradient);
      transform: scaleX(0);
      transition: transform 0.3s ease;
    }

    .cert-card:hover {
      transform: translateY(-3px);
      box-shadow: var(--shadow-xl);
    }

    .cert-card:hover::before {
      transform: scaleX(1);
    }

    .cert-icon {
      width: 48px;
      height: 48px;
      background: rgba(245, 158, 11, 0.1);
      border-radius: var(--radius-md);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      margin-bottom: 1rem;
      transition: all 0.3s ease;
    }

    .cert-card:hover .cert-icon {
      background: var(--accent-gradient);
      transform: scale(1.05);
    }

    .cert-card h3 {
      font-family: var(--font-display);
      font-size: 1.125rem;
      color: var(--primary);
      margin-bottom: 0.25rem;
    }

    .cert-card .org {
      font-weight: 600;
      color: var(--accent);
      font-size: 0.875rem;
      margin-bottom: 0.25rem;
    }

    .cert-card .detail {
      font-size: 0.8125rem;
      color: var(--muted);
      line-height: 1.5;
    }

    .cert-badge {
      display: inline-block;
      background: var(--primary);
      color: white;
      padding: 0.2rem 0.625rem;
      border-radius: var(--radius-sm);
      font-size: 0.6875rem;
      font-weight: 600;
      margin-top: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    /* ------------------------------ CHATTERBOX SECTION --------------------------------*/
    .chatterbox-section {
      padding: 3rem 1.5rem;
      background: var(--light);
    }

    .chatterbox-section .container {
      max-width: 1200px;
      margin: 0 auto;
    }

    .chatterbox-grid {
      display: grid;
      grid-template-columns: 1.2fr 0.8fr;
      gap: 1.5rem;
      margin-top: 2rem;
    }

    .chatterbox-card {
      background: var(--white);
      border-radius: var(--radius-xl);
      padding: 1.75rem;
      box-shadow: var(--shadow-md);
      border: 1px solid var(--border);
      position: relative;
      overflow: hidden;
      transition: all 0.3s ease;
    }

    .chatterbox-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: var(--accent-gradient);
      transform: scaleX(0);
      transition: transform 0.3s ease;
    }

    .chatterbox-card:hover::before {
      transform: scaleX(1);
    }

    .chatterbox-card:hover {
      transform: translateY(-3px);
      box-shadow: var(--shadow-xl);
    }

    .chatterbox-content-main .lead-text {
      font-family: var(--font-display);
      font-size: 1.15rem;
      color: var(--primary);
      line-height: 1.4;
      margin-bottom: 0.875rem;
      font-style: italic;
    }

    .chatterbox-content-main p {
      font-size: 0.9375rem;
      color: var(--text-light);
      line-height: 1.6;
      margin-bottom: 0.875rem;
    }

    .chatterbox-content-main strong {
      color: var(--primary);
      font-weight: 600;
    }

    .insight-box {
      display: flex;
      gap: 0.75rem;
      align-items: flex-start;
      background: linear-gradient(to right, #fffbeb, #fef3c7);
      padding: 1rem;
      border-radius: var(--radius-md);
      margin: 1rem 0;
      border-left: 3px solid var(--accent);
    }

    .insight-icon {
      font-size: 1.25rem;
      flex-shrink: 0;
    }

    .insight-box p {
      font-family: var(--font-display);
      font-size: 1rem;
      color: var(--primary);
      font-style: italic;
      margin: 0;
      line-height: 1.4;
    }

    .age-range {
      font-size: 0.875rem;
      color: var(--muted);
      margin: 0;
    }

    .chatterbox-sidebar {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .chatterbox-quote {
      background: var(--white);
      border: 2px solid var(--border);
      border-radius: var(--radius-xl);
      padding: 1.5rem;
      margin: 0;
      text-align: center;
      position: relative;
      font-family: var(--font-display);
      font-size: 1.1rem;
      font-style: italic;
      color: var(--primary);
      line-height: 1.4;
      transition: all 0.3s ease;
    }

    .chatterbox-quote:hover {
      border-color: var(--accent);
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }

    .quote-mark {
      display: block;
      font-size: 2.5rem;
      color: var(--accent);
      line-height: 1;
      opacity: 0.3;
      font-family: Georgia, serif;
    }

    .quote-mark:first-child {
      margin-bottom: -0.5rem;
    }

    .quote-mark:last-child {
      margin-top: -1rem;
      margin-bottom: -0.25rem;
    }

    .skills-compact {
      background: var(--white);
      border-radius: var(--radius-lg);
      padding: 1.25rem;
      border: 1px solid var(--border);
    }

    .skills-header {
      font-size: 0.8125rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--accent);
      margin-bottom: 0.75rem;
      padding-bottom: 0.5rem;
      border-bottom: 1px solid var(--border);
    }

    .skills-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem;
    }

    .skills-tags span {
      display: inline-flex;
      align-items: center;
      gap: 0.3rem;
      padding: 0.35rem 0.75rem;
      background: var(--light);
      border: 1px solid var(--border);
      border-radius: 50px;
      font-size: 0.8125rem;
      color: var(--primary);
      font-weight: 500;
      transition: all 0.2s ease;
    }

    .skills-tags span::before {
      content: '';
      width: 4px;
      height: 4px;
      background: var(--accent);
      border-radius: 50%;
    }

    .skills-tags span:hover {
      background: var(--accent-gradient);
      border-color: transparent;
      transform: translateY(-1px);
    }

    /* ------------------------------ EXPERTISE GRID --------------------------------*/
    .expertise-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1rem;
      margin-top: 2rem;
    }

    .expertise-card {
      background: var(--white);
      border: 2px solid var(--border);
      border-radius: var(--radius-lg);
      padding: 1.5rem;
      text-align: center;
      transition: all 0.3s ease;
      position: relative;
    }

    .expertise-card::after {
      content: '→';
      position: absolute;
      bottom: 0.75rem;
      right: 0.75rem;
      opacity: 0;
      color: var(--accent);
      transition: all 0.3s ease;
      font-size: 0.875rem;
    }

    .expertise-card:hover {
      border-color: var(--accent);
      transform: translateY(-3px);
      box-shadow: var(--shadow-lg);
    }

    .expertise-card:hover::after {
      opacity: 1;
      right: 1rem;
    }

    .expertise-icon {
      width: 48px;
      height: 48px;
      background: var(--accent-gradient);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 0.875rem;
      font-size: 1.25rem;
    }

    .expertise-card p {
      font-weight: 600;
      color: var(--primary);
      font-size: 0.9375rem;
      line-height: 1.3;
      margin: 0;
    }

    /* ------------------------------ METHODOLOGY --------------------------------*/
    .methodology-section {
      background: var(--white);
    }

    .method-steps {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 1.25rem;
      margin-top: 2rem;
    }

    .method-step {
      text-align: center;
      padding: 1.25rem;
      position: relative;
    }

    .step-number {
      width: 40px;
      height: 40px;
      background: var(--accent-gradient);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1rem;
      font-family: var(--font-display);
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--primary);
    }

    .method-step h3 {
      font-family: var(--font-display);
      font-size: 1.125rem;
      color: var(--primary);
      margin-bottom: 0.5rem;
    }

    .method-step p {
      color: var(--muted);
      font-size: 0.875rem;
      line-height: 1.5;
      margin: 0;
    }

    /* ------------------------------ PROGRAMS --------------------------------*/
    .programs-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 1rem;
      margin-top: 2rem;
    }

    .program-card {
      background: var(--white);
      border-radius: var(--radius-lg);
      padding: 1.5rem;
      box-shadow: var(--shadow-md);
      border-left: 3px solid var(--accent);
      transition: all 0.3s ease;
    }

    .program-card:hover {
      transform: translateX(5px);
      box-shadow: var(--shadow-xl);
    }

    .program-card h3 {
      font-family: var(--font-display);
      font-size: 1.0625rem;
      color: var(--primary);
      margin-bottom: 0.375rem;
    }

    .program-card .duration {
      display: inline-flex;
      align-items: center;
      gap: 0.375rem;
      background: var(--light);
      padding: 0.25rem 0.625rem;
      border-radius: var(--radius-sm);
      font-size: 0.8125rem;
      color: var(--muted);
      margin-bottom: 0.625rem;
    }

    .program-card p {
      color: var(--text-light);
      font-size: 0.875rem;
      line-height: 1.5;
      margin: 0;
    }

    .min-participants {
      text-align: center;
      margin-top: 2rem;
      padding: 1rem;
      background: var(--primary);
      color: white;
      border-radius: var(--radius-md);
      font-weight: 600;
      font-size: 0.9375rem;
    }

    /* ------------------------------ INSTITUTIONS --------------------------------*/
    .institutions-section {
      background: linear-gradient(to right, #fffbeb, #fef3c7);
      padding: 2.5rem 1.5rem;
      margin: 0 1.5rem 3rem;
      border-radius: var(--radius-xl);
      border: 1px dashed var(--accent);
    }

    .institutions-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
      gap: 0.75rem;
      margin-top: 1.5rem;
    }

    .institution-item {
      background: var(--white);
      padding: 0.875rem;
      border-radius: var(--radius-md);
      text-align: center;
      font-weight: 600;
      color: var(--primary);
      box-shadow: var(--shadow-sm);
      transition: all 0.2s ease;
      font-size: 0.9375rem;
    }

    .institution-item:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }

    /* ------------------------------ CTA SECTION --------------------------------*/
    .trainer-cta {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
      padding: 3rem 1.5rem;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .trainer-cta::before {
      content: '';
      position: absolute;
      top: -30%;
      right: -10%;
      width: 300px;
      height: 300px;
      background: radial-gradient(circle, rgba(245, 158, 11, 0.1) 0%, transparent 70%);
    }

    .cta-content {
      position: relative;
      z-index: 1;
      max-width: 700px;
      margin: 0 auto;
    }

    .trainer-cta h2 {
      font-family: var(--font-display);
      font-size: clamp(1.75rem, 4vw, 2.5rem);
      color: white;
      margin-bottom: 1rem;
    }

    .contact-details {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1.5rem;
      margin: 1.5rem 0;
    }

    .contact-item-large {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      color: rgba(255, 255, 255, 0.9);
      font-size: 1rem;
    }

    .contact-icon-large {
      width: 40px;
      height: 40px;
      background: rgba(245, 158, 11, 0.2);
      border-radius: var(--radius-md);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.25rem;
    }

    .cta-btn-large {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      background: var(--accent-gradient);
      color: var(--primary);
      padding: 1rem 2rem;
      border-radius: var(--radius-md);
      font-weight: 700;
      font-size: 1rem;
      text-decoration: none;
      transition: all 0.2s;
      margin-top: 0.75rem;
      box-shadow: 0 8px 20px -8px rgba(245, 158, 11, 0.3);
    }

    .cta-btn-large:hover {
      transform: translateY(-2px);
      box-shadow: 0 12px 30px -10px rgba(245, 158, 11, 0.4);
    }

    /* ------------------------------ RESPONSIVE --------------------------------*/
    @media (max-width: 968px) {
      .chatterbox-grid {
        grid-template-columns: 1fr;
      }

      .chatterbox-sidebar {
        order: -1;
      }
    }

    @media (max-width: 768px) {
      .trainer-hero {
        padding: 3rem 1rem 5rem;
      }

      .trainer-stats {
        gap: 1.5rem;
      }

      .stat-number {
        font-size: 1.75rem;
      }

      .section {
        padding: 2.5rem 1rem;
      }

      .certs-grid,
      .expertise-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
      }

      .expertise-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .institutions-section {
        margin: 0 1rem 2.5rem;
        padding: 2rem 1rem;
      }

      .contact-details {
        flex-direction: column;
        gap: 1rem;
      }

      .method-steps {
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
      }
    }

    @media (max-width: 480px) {
      .expertise-grid {
        grid-template-columns: 1fr;
      }

      .method-steps {
        grid-template-columns: 1fr;
      }
    }

    /* ------------------------------ ANIMATIONS --------------------------------*/
    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .cert-card,
    .expertise-card,
    .program-card,
    .method-step {
      animation: slideUp 0.5s ease-out;
    }

    .reveal {
      opacity: 0;
      transform: translateY(20px);
      transition: all 0.6s ease;
    }

    .reveal.active {
      opacity: 1;
      transform: translateY(0);
    }
  </style>
</head>

<body>

<%- include('partials/header') %>

<!-- HERO SECTION -->
<section class="trainer-hero">
  <div class="trainer-hero-content">
    <div class="badge-affiliation">
      <span>🏆</span>
      Affiliated Trainer – Chatterbox
    </div>

    <div class="trainer-avatar"></div>

    <h1>Mr. Kiran Karampudi</h1>
    <p class="trainer-title">
      Lead Trainer – Communication, Public Speaking, Leadership Skills & Personality Development
    </p>

    <div class="trainer-stats">
      <div class="stat-box">
        <div class="stat-number">8+</div>
        <div class="stat-label">Years Experience</div>
      </div>
      <div class="stat-box">
        <div class="stat-number">100+</div>
        <div class="stat-label">Students Trained</div>
      </div>
      <div class="stat-box">
        <div class="stat-number">50+</div>
        <div class="stat-label">Programs Delivered</div>
      </div>
    </div>
  </div>
  <div class="hero-wave"></div>
</section>

<!-- ABOUT SECTION -->
<section class="about-section">
  <div class="about-content reveal">
    <div class="section-label">About Trainer</div>
    <p class="lead-text">
      "Empowering individuals to communicate with confidence and lead with purpose."
    </p>
    <p class="about-text">
      Kiran Karampudi is a professional communication and leadership skills trainer with over 8 years of experience in training students, business owners, and working professionals. He has extensively trained and mentored business owners, supporting them in strengthening leadership communication, presentation skills, confidence, and professional presence.
    </p>
    <div class="certification-highlight">
      <span>✓</span>
      Certified Train-the-Trainer | ISO 9001:2015 | Startup India Recognized
    </div>
  </div>
</section>

<!-- CERTIFICATIONS -->
<section class="section">
  <div class="section-header reveal">
    <div class="section-label">Credentials</div>
    <h2 class="section-title">Certifications & Recognition</h2>
    <p class="section-subtitle">Internationally recognized qualifications and institutional affiliations.</p>
  </div>

  <div class="certs-grid">
    <div class="cert-card reveal">
      <div class="cert-icon">🎓</div>
      <h3>Certified Train-the-Trainer</h3>
      <p class="org">JD Speakers Institute</p>
      <p class="detail">Public Speaking & Communication Excellence</p>
      <span class="cert-badge">ISO 9001:2015 Certified</span>
    </div>

    <div class="cert-card reveal" style="transition-delay: 100ms;">
      <div class="cert-icon">🌐</div>
      <h3>Member Success Program</h3>
      <p class="org">BNI India</p>
      <p class="detail">World's Largest Business Networking Organization</p>
      <span class="cert-badge">Certified Trainer</span>
    </div>

    <div class="cert-card reveal" style="transition-delay: 200ms;">
      <div class="cert-icon">🏛️</div>
      <h3>Government Recognition</h3>
      <p class="org">Startup India (DPIIT)</p>
      <p class="detail">Department for Promotion of Industry and Internal Trade</p>
      <span class="cert-badge">DPIIT Recognized</span>
    </div>
  </div>
</section>

<!-- CHATTERBOX PORTFOLIO SECTION -->
<section class="chatterbox-section">
  <div class="container">
    <div class="section-header reveal">
      <div class="section-label">Portfolio</div>
      <h2 class="section-title">Chatterbox Speaking Institute – Guntur</h2>
      <p class="section-subtitle">Where hesitant speakers become confident performers</p>
    </div>

    <div class="chatterbox-grid reveal">
      <div class="chatterbox-card">
        <div class="chatterbox-content-main">
          <p class="lead-text">Every transformation begins with a single step onto the stage.</p>
          <p>At <strong>Chatterbox Guntur</strong>, stage fear evolves into commanding presence. Students train, practice, and perform — learning to engage, influence, and inspire audiences with authenticity.</p>
          
          <div class="insight-box">
            <span class="insight-icon">💡</span>
            <p>Confidence builds one speech, one performance, one breakthrough at a time.</p>
          </div>

          <p class="age-range"><strong>Ages 7 to 70</strong> — from young learners to seasoned professionals, everyone finds their voice here.</p>
        </div>
      </div>

      <div class="chatterbox-sidebar">
        <blockquote class="chatterbox-quote">
          <span class="quote-mark">"</span>
          Great speakers are not born — they are trained, guided, and given the right stage.
          <span class="quote-mark">"</span>
        </blockquote>
        
        <div class="skills-compact">
          <div class="skills-header">
            <span>Skills Developed</span>
          </div>
          <div class="skills-tags">
            <span>Stage Confidence</span>
            <span>Expressive Delivery</span>
            <span>Voice Modulation</span>
            <span>Body Language</span>
            <span>Storytelling</span>
            <span>Audience Engagement</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CORE EXPERTISE -->
<section class="section" style="background: var(--light);">
  <div class="section-header reveal">
    <div class="section-label">Specializations</div>
    <h2 class="section-title">Core Training Expertise</h2>
    <p class="section-subtitle">Comprehensive skill development across multiple domains.</p>
  </div>

  <div class="expertise-grid">
    <div class="expertise-card reveal">
      <div class="expertise-icon">🎤</div>
      <p>Public Speaking & Stage Confidence</p>
    </div>
    <div class="expertise-card reveal" style="transition-delay: 50ms;">
      <div class="expertise-icon">💬</div>
      <p>Effective Communication Skills</p>
    </div>
    <div class="expertise-card reveal" style="transition-delay: 100ms;">
      <div class="expertise-icon">👑</div>
      <p>Leadership & Executive Presence</p>
    </div>
    <div class="expertise-card reveal" style="transition-delay: 150ms;">
      <div class="expertise-icon">✨</div>
      <p>Personality Development & Etiquette</p>
    </div>
    <div class="expertise-card reveal" style="transition-delay: 200ms;">
      <div class="expertise-icon">🎯</div>
      <p>Interview Skills & Placement Readiness</p>
    </div>
    <div class="expertise-card reveal" style="transition-delay: 250ms;">
      <div class="expertise-icon">📊</div>
      <p>Presentation & Group Discussion Skills</p>
    </div>
    <div class="expertise-card reveal" style="transition-delay: 300ms;">
      <div class="expertise-icon">🤝</div>
      <p>Body Language & Confidence Building</p>
    </div>
    <div class="expertise-card reveal" style="transition-delay: 400ms;">
      <div class="expertise-icon">🗣️</div>
      <p>Public Speaking & Communication Skills</p>
    </div>
  </div>
</section>

<!-- METHODOLOGY -->
<section class="section methodology-section">
  <div class="section-header reveal">
    <div class="section-label">Approach</div>
    <h2 class="section-title">Training Methodology</h2>
    <p class="section-subtitle">Practical, interactive, and results-driven learning experiences.</p>
  </div>

  <div class="method-steps">
    <div class="method-step reveal">
      <div class="step-number">1</div>
      <h3>Interactive Learning</h3>
      <p>Activity-based sessions that engage participants actively rather than passive listening.</p>
    </div>
    <div class="method-step reveal" style="transition-delay: 100ms;">
      <div class="step-number">2</div>
      <h3>Live Practice</h3>
      <p>Real speaking opportunities with structured, constructive feedback for rapid improvement.</p>
    </div>
    <div class="method-step reveal" style="transition-delay: 200ms;">
      <div class="step-number">3</div>
      <h3>Leadership Sims</h3>
      <p>Role-plays and simulations based on real business scenarios and leadership challenges.</p>
    </div>
    <div class="method-step reveal" style="transition-delay: 300ms;">
      <div class="step-number">4</div>
      <h3>Practical Frameworks</h3>
      <p>Proven techniques used by successful business leaders and executives worldwide.</p>
    </div>
    <div class="method-step reveal" style="transition-delay: 400ms;">
      <div class="step-number">5</div>
      <h3>Tailored Approach</h3>
      <p>Customized confidence-building techniques matched to each participant's level and goals.</p>
    </div>
  </div>
</section>

<!-- PROGRAMS OFFERED -->
<section class="section" style="background: var(--light);">
  <div class="section-header reveal">
    <div class="section-label">Programs</div>
    <h2 class="section-title">Training Programs Offered</h2>
    <p class="section-subtitle">Flexible formats designed for maximum impact and learning retention.</p>
  </div>

  <div class="programs-grid">
    <div class="program-card reveal">
      <h3>One-Day Intensive</h3>
      <div class="duration">
        <svg width="14" height="14" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        5-6 Hours
      </div>
      <p>Intensive crash course covering fundamentals with immediate practical application.</p>
    </div>
    <div class="program-card reveal" style="transition-delay: 100ms;">
      <h3>Leadership Development</h3>
      <div class="duration">
        <svg width="14" height="14" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        Multi-Week
      </div>
      <p>Comprehensive workshops focusing on communication and executive presence.</p>
    </div>
    <div class="program-card reveal" style="transition-delay: 200ms;">
      <h3>Executive Presence</h3>
      <div class="duration">
        <svg width="14" height="14" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        Custom
      </div>
      <p>Advanced public speaking and leadership programs for senior professionals.</p>
    </div>
  </div>

  <div class="min-participants reveal">
    👥 Minimum Batch Size: 100 Participants
  </div>
</section>

<!-- Industries Served -->
<section class="institutions-section reveal">
  <div class="section-header">
    <div class="section-label">Clients</div>
    <h2 class="section-title">Industries Served</h2>
  </div>

  <div class="institutions-grid">
    <div class="institution-item">🏨 Hospitality</div>
    <div class="institution-item">💼 Sales</div>
    <div class="institution-item">📢 Marketing</div>
    <div class="institution-item">🏢 Corporate Offices</div>
    <div class="institution-item">⚙️ Operations</div>
    <div class="institution-item">🏛️ Engineering Colleges</div>
    <div class="institution-item">📚 Degree Colleges</div>
    <div class="institution-item">🎓 Management Institutes</div>
    <div class="institution-item">🏫 Junior Colleges</div>
    <div class="institution-item">📖 CBSE Schools</div>
    <div class="institution-item">📝 ICSE Schools</div>
    <div class="institution-item">🏫 State Board Schools</div>  
  </div>
</section>

<!-- CTA SECTION -->
<section class="trainer-cta">
  <div class="cta-content reveal">
    <h2>Book a Training Program</h2>
    <p style="color: rgba(255,255,255,0.8); font-size: 1rem; margin-bottom: 1.5rem;">
      Ready to transform your students or employees into confident communicators? Get in touch today.
    </p>

    <div class="contact-details">
      <div class="contact-item-large">
        <div class="contact-icon-large">📞</div>
        <div>
          <div style="font-size: 0.8125rem; color: rgba(255,255,255,0.6);">Call Directly</div>
          <div style="font-weight: 700;">+91 7799677888</div>
        </div>
      </div>

      <div class="contact-item-large">
        <div class="contact-icon-large">✉️</div>
        <div>
          <div style="font-size: 0.8125rem; color: rgba(255,255,255,0.6);">Email Us</div>
          <div style="font-weight: 700;">brighttalksacademy@gmail.com</div>
        </div>
      </div>
    </div>

    <a href="/contact" class="cta-btn-large">
      <span>Enquire Now</span>
      <svg width="18" height="18" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
      </svg>
    </a>
  </div>
</section>

<%- include('partials/footer') %>

<script>
  // Scroll reveal animation
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -30px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('active');
      }
    });
  }, observerOptions);

  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>

</body>
</html>