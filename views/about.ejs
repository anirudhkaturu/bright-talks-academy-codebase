<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>About | The Bright Talks Academy</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">

  <style>
    /* ------------------------------
   ROOT VARIABLES
--------------------------------*/
:root {
  --primary: #0a2f5c;
  --accent: #fbbf24;
  --dark: #1f2937;
  --muted: #6b7280;
  --light: #f9fafb;
  --white: #ffffff;
  --shadow: rgba(0,0,0,0.08);
}

/* ------------------------------
   RESET
--------------------------------*/
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Segoe UI", system-ui, sans-serif;
}
body {
  background: var(--light);
  color: var(--dark);
  line-height: 1.7;
}
a {
  text-decoration: none;
}

/* ------------------------------
   GLOBAL SECTIONS
--------------------------------*/
section {
  padding: 80px 60px;
}
h1, h2, h3 { color: var(--primary); font-weight: 700; }
h1 { font-size: 3rem; margin-bottom: 20px; }
h2 { font-size: 2.4rem; margin-bottom: 20px; }
h3 { font-size: 1.4rem; margin-bottom: 10px; }
p { color: var(--muted); max-width: 900px; margin-bottom: 16px; }

/* ------------------------------
   CTA BUTTON
--------------------------------*/
.cta {
  display: inline-block;
  background: var(--accent);
  color: #000;
  padding: 12px 24px;
  border-radius: 8px;
  font-weight: 600;
  transition: transform 0.2s ease, background 0.2s ease;
}
.cta:hover {
  background: #fcd34d;
  transform: translateY(-2px);
}

/* ------------------------------
   HERO SECTION
--------------------------------*/
.about-hero {
  text-align: center;           /* centers all content */
  margin-bottom: 60px;
  display: flex;
  flex-direction: column;
  align-items: center;          /* centers horizontally */
  justify-content: center;
  gap: 16px;                    /* space between h1 and paragraph */
  max-width: 900px;             /* optional: prevent paragraph from stretching too wide */
  margin-left: auto;
  margin-right: auto;           /* centers the hero section in the page */
}

.about-hero h1 {
  font-size: 3.2rem;
  margin-bottom: 16px;
}

.about-hero p {
  font-size: 1.2rem;
  color: var(--muted);
  line-height: 1.6;
  text-align: center;           /* explicitly center text */
  max-width: 700px;             /* limits width for better readability */
}

/* ------------------------------
   STORY
--------------------------------*/
.about-story {
  max-width: 850px;
  margin: 0 auto 60px;
  text-align: center;
}

/* ------------------------------
   STATS
--------------------------------*/
.about-stats {
  display: flex;
  justify-content: center;
  gap: 40px;
  flex-wrap: wrap;
  margin-bottom: 60px;
}
.stat {
  background: var(--white);
  padding: 30px 20px;
  border-radius: 12px;
  box-shadow: 0 8px 20px var(--shadow);
  text-align: center;
  min-width: 150px;
  transition: transform 0.3s ease;
}
.stat:hover {
  transform: translateY(-6px);
}
.stat h3 {
  color: var(--accent);
  font-size: 2rem;
  margin-bottom: 8px;
}
.stat p {
  font-weight: 600;
}

/* ------------------------------
   PHILOSOPHY
--------------------------------*/
.philosophy-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  margin-bottom: 60px;
}
.philosophy-grid .card {
  background: var(--white);
  padding: 20px;
  border-radius: 12px;
  box-shadow: 0 6px 20px var(--shadow);
  text-align: center;
  transition: transform 0.3s ease;
}
.philosophy-grid .card:hover {
  transform: translateY(-6px);
}

/* ------------------------------
   PROGRAMS
--------------------------------*/
.program-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  margin-bottom: 60px;
}
.program-card {
  background: var(--white);
  padding: 20px;
  border-radius: 12px;
  box-shadow: 0 6px 20px var(--shadow);
  font-weight: 600;
  text-align: center;
  transition: transform 0.3s ease;
}
.program-card:hover {
  transform: translateY(-6px);
}

/* ------------------------------
   WHY US
--------------------------------*/
.about-why {
  max-width: 800px;
  margin: 0 auto 60px;
}
.about-why ul {
  list-style: none;
  padding-left: 0;
}
.about-why li {
  margin-bottom: 12px;
  font-weight: 500;
  position: relative;
  padding-left: 28px;
  color: var(--muted);
}
.about-why li::before {
  content: '✔';
  position: absolute;
  left: 0;
  top: 0;
  color: var(--accent);
}

/* ------------------------------
   CTA SECTION
--------------------------------*/
.about-cta {
  text-align: center;
}
.about-cta h2 {
  font-size: 2.4rem;
  margin-bottom: 16px;
}
.about-cta p {
  font-size: 1.1rem;
  margin-bottom: 20px;
}
.about-cta .cta {
  font-size: 1rem;
}

/* ------------------------------
   RESPONSIVE
--------------------------------*/
@media (max-width: 900px) {
  section { padding: 60px 30px; }
  .about-hero h1 { font-size: 2.6rem; }
}
@media (max-width: 600px) {
  .about-stats { flex-direction: column; gap: 20px; }
  .philosophy-grid, .program-grid { grid-template-columns: 1fr; }
}

  </style>
</head>

<body>

<%- include('partials/header') %>

<!-- HERO -->
<section class="about-hero">
  <h1>We Create Confident Speakers</h1>
  <p>At The Bright Talks Academy, we help individuals transform fear into confidence and ideas into powerful communication.</p>
  <a href="/contact" class="cta">Book a Free Call</a>
</section>

<!-- STORY -->
<section class="about-story">
  <div class="container-narrow">
    <h2>Our Story</h2>
    <p>Communication changes lives — yet most people are never taught how to express themselves confidently. The Bright Talks Academy was created to bridge this gap.</p>
    <p>We work with students, professionals, and aspiring leaders who want to speak clearly, present confidently, and communicate with impact in real-world situations.</p>
  </div>
</section>

<!-- STATS -->
<section class="about-stats">
  <div class="stat"><h3>100+</h3><p>Speakers Trained</p></div>
  <div class="stat"><h3>10+</h3><p>Skill-Based Programs</p></div>
  <div class="stat"><h3>1-on-1</h3><p>Personal Coaching</p></div>
</section>

<!-- PHILOSOPHY -->
<section class="about-philosophy">
  <h2>How We Teach</h2>
  <div class="philosophy-grid">
    <div class="card"><h3>Practice First</h3><p>We focus on real speaking practice instead of theory-heavy lessons. Learners improve by doing, not memorizing.</p></div>
    <div class="card"><h3>Personal Feedback</h3><p>Every student receives structured feedback to improve clarity, confidence, and expression.</p></div>
    <div class="card"><h3>Real-World Skills</h3><p>From interviews to presentations, our training prepares learners for real communication challenges.</p></div>
  </div>
</section>

<!-- PROGRAMS -->
<section class="about-programs">
  <h2>What We Offer</h2>
  <div class="program-grid">
    <div class="program-card">Public Speaking & Stage Presence</div>
    <div class="program-card">Corporate & Professional Communication</div>
    <div class="program-card">Confidence & Personality Development</div>
    <div class="program-card">Leadership & Expressive Communication</div>
  </div>
</section>

<!-- WHY US -->
<section class="about-why">
  <h2>Why Choose Bright Talks Academy</h2>
  <ul>
    <li>Confidence-focused learning approach</li>
    <li>Safe, supportive speaking environment</li>
    <li>Personalized improvement plans</li>
    <li>Designed for students & professionals</li>
  </ul>
</section>

<!-- CTA -->
<section class="about-cta">
  <h2>Ready to Speak with Confidence?</h2>
  <p>Join The Bright Talks Academy and take the first step toward confident, impactful communication.</p>
  <a href="/contact" class="cta">Start Your Journey</a>
</section>

<%- include('partials/footer') %>

</body>
</html>
