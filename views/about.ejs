<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>About | The Bright Talks Academy</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">

  <style>
    /* ------------------------------ ROOT VARIABLES --------------------------------*/
    :root {
      --primary: #0f172a;
      --primary-light: #1e293b;
      --accent: #f59e0b;
      --accent-light: #fbbf24;
      --accent-gradient: linear-gradient(135deg, #f59e0b 0%, #fbbf24 100%);
      --dark: #0f172a;
      --muted: #64748b;
      --light: #f8fafc;
      --white: #ffffff;
      --cream: #fefdfc;
      --border: #e2e8f0;
      --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
      --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
      --shadow-lg: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
      --shadow-colored: 0 20px 40px -5px rgba(245, 158, 11, 0.15);
      --radius-sm: 8px;
      --radius-md: 12px;
      --radius-lg: 16px;
      --radius-xl: 24px;

      --font-sans: 'Inter', system-ui, -apple-system, sans-serif;
      --font-display: 'Playfair Display', Georgia, serif;
    }

    /* ------------------------------ RESET & BASE --------------------------------*/
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: var(--font-sans);
      background: var(--cream);
      color: var(--dark);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    ::selection {
      background: var(--accent);
      color: white;
    }

    img {
      max-width: 100%;
      height: auto;
      display: block;
    }

    /* ------------------------------ UTILITIES --------------------------------*/
    .container {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
    }

    .section-header {
      max-width: 700px;
      margin: 0 auto 2rem;
      text-align: center;
    }

    .section-label {
      display: inline-block;
      font-size: 0.875rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--accent);
      margin-bottom: 0.5rem;
    }

    h2 {
      font-family: var(--font-display);
      font-size: clamp(1.75rem, 3.5vw, 2.25rem);
      font-weight: 700;
      color: var(--primary);
      line-height: 1.2;
      margin-bottom: 0.5rem;
    }

    .subtitle {
      font-size: 1rem;
      color: var(--muted);
      line-height: 1.6;
      margin-bottom: 0;
    }

    /* ------------------------------ HERO SECTION --------------------------------*/
    .about-hero {
      position: relative;
      padding: 6rem 0 3rem;
      background: linear-gradient(135deg, var(--cream) 0%, #fffbeb 50%, var(--cream) 100%);
      overflow: hidden;
      text-align: center;
    }

    .about-hero::before {
      content: '';
      position: absolute;
      top: -30%;
      left: 50%;
      transform: translateX(-50%);
      width: 800px;
      height: 800px;
      background: radial-gradient(circle, rgba(245, 158, 11, 0.08) 0%, transparent 70%);
      pointer-events: none;
    }

    .about-hero .container {
      position: relative;
      z-index: 1;
    }

    .about-hero .section-label {
      margin-bottom: 0.75rem;
    }

    .about-hero h1 {
      font-family: var(--font-display);
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: 700;
      color: var(--primary);
      line-height: 1.1;
      margin-bottom: 0.75rem;
      letter-spacing: -0.02em;
    }

    .about-hero h1 span {
      color: var(--accent);
      position: relative;
      display: inline-block;
    }

    .about-hero h1 span::after {
      content: '';
      position: absolute;
      bottom: 2px;
      left: -2px;
      right: -2px;
      height: 6px;
      background: rgba(245, 158, 11, 0.15);
      z-index: -1;
      border-radius: 4px;
    }

    .about-hero p {
      font-size: 1.1rem;
      color: var(--muted);
      max-width: 600px;
      margin: 0 auto 1.5rem;
      line-height: 1.6;
    }

    .cta {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      background: var(--accent-gradient);
      color: var(--primary);
      padding: 0.875rem 1.75rem;
      border-radius: var(--radius-md);
      font-weight: 600;
      font-size: 0.95rem;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 4px 14px rgba(245, 158, 11, 0.3);
    }

    .cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(245, 158, 11, 0.4);
      gap: 0.75rem;
    }

    /* ------------------------------ STORY SECTION --------------------------------*/
    .about-story {
      padding: 3rem 0;
      background: var(--white);
    }

    .story-content {
      max-width: 800px;
      margin: 0 auto;
      text-align: center;
    }

    .story-content h2 {
      margin-bottom: 1rem;
    }

    .story-text {
      font-size: 1rem;
      color: var(--muted);
      line-height: 1.8;
      margin-bottom: 1rem;
    }

    .story-highlight {
      background: linear-gradient(135deg, rgba(245, 158, 11, 0.08) 0%, rgba(245, 158, 11, 0.02) 100%);
      border: 1px solid rgba(245, 158, 11, 0.15);
      padding: 1.5rem;
      margin: 1.5rem 0 0;
      text-align: center;
      border-radius: var(--radius-lg);
    }

    .story-highlight p {
      font-family: var(--font-display);
      font-size: 1.15rem;
      color: var(--primary);
      font-style: italic;
      margin: 0;
      line-height: 1.5;
    }

    /* ------------------------------ STATS SECTION --------------------------------*/
    .about-stats {
      padding: 2.5rem 0;
      background: var(--primary);
      position: relative;
      overflow: hidden;
    }

    .about-stats::before {
      content: '';
      position: absolute;
      top: 0;
      right: 0;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(245, 158, 11, 0.1) 0%, transparent 70%);
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      position: relative;
      z-index: 1;
    }

    .stat {
      text-align: center;
      padding: 0.5rem;
    }

    .stat-number {
      font-family: var(--font-display);
      font-size: 2.5rem;
      font-weight: 700;
      color: var(--accent);
      line-height: 1;
      margin-bottom: 0.25rem;
      display: block;
    }

    .stat-label {
      font-size: 0.875rem;
      color: rgba(255, 255, 255, 0.8);
      font-weight: 500;
      letter-spacing: 0.02em;
    }

    /* ------------------------------ PHILOSOPHY SECTION --------------------------------*/
    .about-philosophy {
      padding: 3rem 0;
      background: var(--cream);
    }

    .philosophy-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1rem;
    }

    .philosophy-card {
      background: var(--white);
      padding: 1.75rem 1.5rem;
      border-radius: var(--radius-xl);
      border: 1px solid var(--border);
      text-align: center;
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .philosophy-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 3px;
      background: var(--accent-gradient);
      transform: scaleX(0);
      transform-origin: center;
      transition: transform 0.4s ease;
    }

    .philosophy-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-md);
      border-color: transparent;
    }

    .philosophy-card:hover::before {
      transform: scaleX(1);
    }

    .philosophy-icon {
      width: 48px;
      height: 48px;
      background: rgba(245, 158, 11, 0.1);
      border-radius: var(--radius-lg);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 0.75rem;
      font-size: 1.5rem;
      transition: all 0.3s ease;
    }

    .philosophy-card:hover .philosophy-icon {
      background: var(--accent-gradient);
      transform: scale(1.05);
    }

    .philosophy-card h3 {
      font-family: var(--font-display);
      font-size: 1.15rem;
      color: var(--primary);
      margin-bottom: 0.5rem;
    }

    .philosophy-card p {
      color: var(--muted);
      font-size: 0.95rem;
      line-height: 1.6;
      margin: 0;
    }

    /* ------------------------------ PROGRAMS SECTION --------------------------------*/
    .about-programs {
      padding: 3rem 0;
      background: var(--white);
    }

    .programs-wrapper {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 1rem;
    }

    .program-card {
      background: var(--white);
      border: 2px solid var(--border);
      padding: 1.5rem 1.25rem;
      border-radius: var(--radius-lg);
      font-weight: 600;
      text-align: center;
      transition: all 0.3s ease;
      cursor: pointer;
      position: relative;
      overflow: hidden;
    }

    .program-card::after {
      content: '→';
      position: absolute;
      bottom: 1rem;
      right: 1rem;
      opacity: 0;
      transform: translateX(-10px);
      transition: all 0.3s ease;
      color: var(--accent);
      font-size: 1.25rem;
    }

    .program-card:hover {
      border-color: var(--accent);
      transform: translateY(-3px);
      box-shadow: var(--shadow-sm);
    }

    .program-card:hover::after {
      opacity: 1;
      transform: translateX(0);
    }

    .program-icon {
      font-size: 1.75rem;
      margin-bottom: 0.5rem;
      display: block;
    }

    .program-card h4 {
      font-family: var(--font-display);
      font-size: 1rem;
      color: var(--primary);
      margin: 0;
      line-height: 1.4;
    }

    /* ------------------------------ WHY US SECTION --------------------------------*/
    .about-why {
      padding: 3rem 0;
      background: linear-gradient(to bottom, var(--cream), var(--white));
    }

    .why-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 1rem;
    }

    .why-item {
      display: flex;
      gap: 1rem;
      padding: 1.25rem;
      background: var(--white);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border);
      transition: all 0.3s ease;
    }

    .why-item:hover {
      box-shadow: var(--shadow-sm);
      transform: translateX(3px);
      border-color: var(--accent);
    }

    .why-icon {
      width: 40px;
      height: 40px;
      min-width: 40px;
      background: var(--accent-gradient);
      border-radius: var(--radius-md);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.1rem;
      box-shadow: 0 4px 12px rgba(245, 158, 11, 0.3);
    }

    .why-content h3 {
      font-family: var(--font-display);
      font-size: 1.05rem;
      color: var(--primary);
      margin-bottom: 0.25rem;
    }

    .why-content p {
      color: var(--muted);
      font-size: 0.9rem;
      line-height: 1.5;
      margin: 0;
    }

    /* ------------------------------ CTA SECTION --------------------------------*/
    .about-cta {
      padding: 3rem 0;
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .about-cta::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -10%;
      width: 600px;
      height: 600px;
      background: radial-gradient(circle, rgba(245, 158, 11, 0.1) 0%, transparent 70%);
    }

    .cta-content {
      position: relative;
      z-index: 1;
      max-width: 600px;
      margin: 0 auto;
    }

    .about-cta h2 {
      color: var(--white);
      margin-bottom: 0.5rem;
      font-size: clamp(1.5rem, 3vw, 2rem);
    }

    .about-cta p {
      color: rgba(255, 255, 255, 0.8);
      font-size: 1rem;
      margin-bottom: 1.5rem;
      line-height: 1.5;
    }

    .about-cta .cta {
      background: var(--white);
      color: var(--primary);
      font-size: 0.95rem;
      padding: 0.875rem 1.75rem;
    }

    .about-cta .cta:hover {
      background: var(--accent);
      color: var(--primary);
    }

    /* ------------------------------ ANIMATIONS --------------------------------*/
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .reveal {
      opacity: 0;
      transform: translateY(15px);
      transition: all 0.5s ease;
    }

    .reveal.active {
      opacity: 1;
      transform: translateY(0);
    }

    /* ------------------------------ RESPONSIVE --------------------------------*/
    @media (max-width: 1024px) {
      .stats-grid {
        gap: 1rem;
      }

      .stat-number {
        font-size: 2rem;
      }
    }

    @media (max-width: 768px) {
      .container {
        padding: 0 1.25rem;
      }

      .about-hero {
        padding: 4rem 0 2rem;
      }

      .about-story,
      .about-stats,
      .about-philosophy,
      .about-programs,
      .about-why,
      .about-cta {
        padding: 2rem 0;
      }

      .section-header {
        margin-bottom: 1.5rem;
      }

      .philosophy-grid,
      .programs-wrapper,
      .why-grid {
        grid-template-columns: 1fr;
        gap: 0.75rem;
      }

      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 0.75rem;
      }

      .stat {
        padding: 0.5rem;
      }

      .story-highlight {
        padding: 1.25rem;
        margin-top: 1.25rem;
      }

      .story-highlight p {
        font-size: 1.05rem;
      }

      .about-hero p {
        font-size: 1rem;
      }
    }

    @media (max-width: 480px) {
      .stats-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>

<body>

<%- include('partials/header') %>

<!-- HERO -->
<section class="about-hero">
  <div class="container">
    <div class="section-label">About Us</div>
    <h1>We Create <span>Confident Speakers</span></h1>
    <p>At The Bright Talks Academy, we help individuals transform fear into confidence and ideas into powerful communication that inspires action.</p>
    <a href="/contact" class="cta">Book a Free Discovery Call →</a>
  </div>
</section>

<!-- STORY -->
<section class="about-story">
  <div class="container">
    <div class="story-content reveal">
      <div class="section-header">
        <div class="section-label">Our Story</div>
        <h2>Bridging the Communication Gap</h2>
      </div>

      <p class="story-text">Communication changes lives — yet most people are never taught how to express themselves confidently. The Bright Talks Academy was created to bridge this gap between potential and expression.</p>
      <p class="story-text">We work with students, professionals, and aspiring leaders who want to speak clearly, present confidently, and communicate with impact in real-world situations.</p>

      <div class="story-highlight">
        <p>"Every great speaker was once a beginner who refused to give up."</p>
      </div>
    </div>
  </div>
</section>

<!-- STATS -->
<section class="about-stats">
  <div class="container">
    <div class="stats-grid reveal">
      <div class="stat">
        <span class="stat-number">100+</span>
        <span class="stat-label">Speakers Trained</span>
      </div>
      <div class="stat">
        <span class="stat-number">10+</span>
        <span class="stat-label">Skill Programs</span>
      </div>
      <div class="stat">
        <span class="stat-number">1:1</span>
        <span class="stat-label">Personal Coaching</span>
      </div>
      <div class="stat">
        <span class="stat-number">98%</span>
        <span class="stat-label">Success Rate</span>
      </div>
    </div>
  </div>
</section>

<!-- PHILOSOPHY -->
<section class="about-philosophy">
  <div class="container">
    <div class="section-header reveal">
      <div class="section-label">Our Method</div>
      <h2>How We Teach</h2>
      <p class="subtitle">Our proven methodology focuses on practical application, personalized feedback, and real-world results.</p>
    </div>

    <div class="philosophy-grid">
      <div class="philosophy-card reveal" style="transition-delay: 100ms;">
        <div class="philosophy-icon">🎯</div>
        <h3>Practice First</h3>
        <p>We focus on real speaking practice instead of theory-heavy lessons. Learners improve by doing, not memorizing.</p>
      </div>
      <div class="philosophy-card reveal" style="transition-delay: 200ms;">
        <div class="philosophy-icon">💬</div>
        <h3>Personal Feedback</h3>
        <p>Every student receives structured feedback to improve clarity, confidence, and expression in every session.</p>
      </div>
      <div class="philosophy-card reveal" style="transition-delay: 300ms;">
        <div class="philosophy-icon">🚀</div>
        <h3>Real-World Skills</h3>
        <p>From interviews to presentations, our training prepares learners for actual communication challenges.</p>
      </div>
    </div>
  </div>
</section>

<!-- PROGRAMS -->
<section class="about-programs">
  <div class="container">
    <div class="section-header reveal">
      <div class="section-label">Offerings</div>
      <h2>What We Offer</h2>
      <p class="subtitle">Comprehensive programs designed to elevate every aspect of your communication abilities.</p>
    </div>

    <div class="programs-wrapper reveal">
      <div class="program-card">
        <span class="program-icon">🎤</span>
        <h4>Public Speaking & Stage Presence</h4>
      </div>
      <div class="program-card">
        <span class="program-icon">💼</span>
        <h4>Corporate & Professional Communication</h4>
      </div>
      <div class="program-card">
        <span class="program-icon">✨</span>
        <h4>Confidence & Personality Development</h4>
      </div>
      <div class="program-card">
        <span class="program-icon">👑</span>
        <h4>Leadership & Expressive Communication</h4>
      </div>
    </div>
  </div>
</section>

<!-- WHY US -->
<section class="about-why">
  <div class="container">
    <div class="section-header reveal">
      <div class="section-label">Why Us</div>
      <h2>Why Choose Bright Talks Academy</h2>
      <p class="subtitle">We provide a supportive environment where growth happens naturally and confidence builds steadily.</p>
    </div>

    <div class="why-grid">
      <div class="why-item reveal" style="transition-delay: 100ms;">
        <div class="why-icon">🛡️</div>
        <div class="why-content">
          <h3>Confidence-Focused Learning</h3>
          <p>Our curriculum is designed to build self-assurance from day one, ensuring you feel empowered in every interaction.</p>
        </div>
      </div>
      <div class="why-item reveal" style="transition-delay: 200ms;">
        <div class="why-icon">🤝</div>
        <div class="why-content">
          <h3>Safe, Supportive Environment</h3>
          <p>Practice without judgment in a nurturing space where mistakes become stepping stones to mastery.</p>
        </div>
      </div>
      <div class="why-item reveal" style="transition-delay: 300ms;">
        <div class="why-icon">📋</div>
        <div class="why-content">
          <h3>Personalized Improvement Plans</h3>
          <p>Every learner receives a customized roadmap tailored to their specific goals and current skill level.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="about-cta">
  <div class="container">
    <div class="cta-content reveal">
      <h2>Ready to Speak with Confidence?</h2>
      <p>Join The Bright Talks Academy and take the first step toward confident, impactful communication.</p>
      <a href="/contact" class="cta">Start Your Journey Today →</a>
    </div>
  </div>
</section>

<%- include('partials/footer') %>

<script>
  // Scroll reveal animation
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -20px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('active');
      }
    });
  }, observerOptions);

  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>

</body>
</html>